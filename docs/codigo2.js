  // Supongamos que tienes una variable llamada "disponible" que es verdadera o falsa según la disponibilidad del producto/servicio

  var disponible = true; // Cambia esto según tu lógica

  var availabilityAlert = document.getElementById("alert");
  var availabilityMessage = document.getElementById("alert-message");
  
  if (disponible) {
      availabilityMessage.textContent = "¡DISPONIBLE!";
      availabilityAlert.style.backgroundColor = "#c8e6c9"; // Color para disponibilidad
  } else {
      availabilityMessage.textContent = "¡NO DISPONIBLE!";
      availabilityAlert.style.backgroundColor = "#ffcdd2"; // Color para no disponibilidad
  }
  
  availabilityAlert.style.display = "block"; // Mostrar la alerta


    // Supongamos que tienes una variable llamada "disponible" que es verdadera o falsa según la disponibilidad del producto/servicio

    var disponible = true; // Cambia esto según tu lógica

    var availabilityAlert = document.getElementById("alert1");
    var availabilityMessage = document.getElementById("alert-message1");
    
    if (disponible) {
        availabilityMessage.textContent = "¡DISPONIBLE!";
        availabilityAlert.style.backgroundColor = "#c8e6c9"; // Color para disponibilidad
    } else {
        availabilityMessage.textContent = "¡NO DISPONIBLE!";
        availabilityAlert.style.backgroundColor = "#ffcdd2"; // Color para no disponibilidad
    }
    
    availabilityAlert.style.display = "block"; // Mostrar la alerta


        // Supongamos que tienes una variable llamada "disponible" que es verdadera o falsa según la disponibilidad del producto/servicio

        var disponible = true; // Cambia esto según tu lógica

        var availabilityAlert = document.getElementById("alert2");
        var availabilityMessage = document.getElementById("alert-message2");
        
        if (disponible) {
            availabilityMessage.textContent = "¡DISPONIBLE!";
            availabilityAlert.style.backgroundColor = "#c8e6c9"; // Color para disponibilidad
        } else {
            availabilityMessage.textContent = "¡NO DISPONIBLE!";
            availabilityAlert.style.backgroundColor = "#ffcdd2"; // Color para no disponibilidad
        }
        
        availabilityAlert.style.display = "block"; // Mostrar la alerta

    // Supongamos que tienes una variable llamada "disponible" que es verdadera o falsa según la disponibilidad del producto/servicio

    var disponible = true; // Cambia esto según tu lógica

    var availabilityAlert = document.getElementById("alert3");
    var availabilityMessage = document.getElementById("alert-message3");
    
    if (disponible) {
        availabilityMessage.textContent = "¡DISPONIBLE!";
        availabilityAlert.style.backgroundColor = "#c8e6c9"; // Color para disponibilidad
    } else {
        availabilityMessage.textContent = "¡NO DISPONIBLE!";
        availabilityAlert.style.backgroundColor = "#ffcdd2"; // Color para no disponibilidad
    }
    
    availabilityAlert.style.display = "block"; // Mostrar la alerta



    // Supongamos que tienes una variable llamada "disponible" que es verdadera o falsa según la disponibilidad del producto/servicio

    var disponible = false; // Cambia esto según tu lógica

    var availabilityAlert = document.getElementById("alert4");
    var availabilityMessage = document.getElementById("alert-message4");
    
    if (disponible) {
        availabilityMessage.textContent = "¡DISPONIBLE!";
        availabilityAlert.style.backgroundColor = "#c8e6c9"; // Color para disponibilidad
    } else {
        availabilityMessage.textContent = "¡NO DISPONIBLE!";
        availabilityAlert.style.backgroundColor = "#ffcdd2"; // Color para no disponibilidad
    }
    
    availabilityAlert.style.display = "block"; // Mostrar la alerta